# 📚 Bookstore DevOps Educational Project

โครงการนี้สร้างขึ้นเพื่อใช้สำหรับการเรียนการสอนในสายงาน **DevOps Engineer** เริ่มต้น เพื่อให้เห็นภาพการ Build, Containerization และ Deployment

## 🚀 Features
- **Frontend**: แสดงรายการหนังสือ และรูปหน้าปก แบบ Responsive
- **Backend (API)**: จัดการข้อมูลหนังสือ (ดึงข้อมูล, เพิ่ม, ลบ)
- **Database**: จัดเก็บข้อมูลด้วย PostgreSQL
- **Containerization**: รองรับการรันด้วย Docker Compose

## 🛠 Project Structure
- `/frontend`: HTML, CSS, JavaScript (Nginx)
- `/backend`: Node.js Express API
- `/database`: SQL scripts สำหรับ Initial Database
- `docker-compose.yml`: ไฟล์ควบคุมการรันทั้งระบบ

## 🏃 How to Run (Local)

### Prerequisites
- [Docker](https://www.docker.com/products/docker-desktop/) installed
- [Docker Compose](https://docs.docker.com/compose/install/) installed

### Steps
1. Clone หรือ Download project นี้
2. เปิด Terminal ใน Folder ของ project
3. รันคำสั่ง:
   ```bash
   docker-compose up --build
   ```
4. เข้าดูหน้าเว็บที่: [http://localhost:8080](http://localhost:8080)
5. ทดสอบเพิ่ม/ลบ หนังสือผ่านหน้าเว็บ

## 🎓 DevOps Learning Path
โครงการนี้ออกแบบมาให้ฝึกฝนต่อยอดในหัวข้อต่างๆ ดังนี้:
1. **Docker**: เรียนรู้การเขียน Dockerfile ของ Frontend และ Backend
2. **Docker Compose**: เรียนรู้การทำ Service Orchestration และ Network ระหว่าง Container
3. **CI/CD**: ฝึกทำ Pipeline เพื่อ Build Image และ Push ไปยัง Registry
4. **Kubernetes**: นำไฟล์ YAML ไปแปลงเป็น K8s Manifests
5. **Helm**: สร้าง Helm Chart เพื่อจัดการ Deployment
6. **Serverless**: ฝึกนำ Backend ไปรันบน AWS Lambda

## 💡 Note สำหรับผู้สอน
- Source code เขียนให้เข้าใจง่ายที่สุด (Simple Logic)
- สามารถให้นักเรียนฝึกแก้ไข UI หรือ API เพื่อทดสอบการเปลี่ยนแปลง (Hot Reload/Rebuild)
- สามารถจำลองสถานการณ์ Database Connection Error เพื่อฝึก Troubleshooting
