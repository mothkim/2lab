# 🎓 Helm Template Syntax สำหรับผู้เริ่มต้น

ยินดีต้อนรับสู่บทเรียนพื้นฐานของ Helm! โฟลเดอร์นี้รวบรวม Syntax ที่จำเป็นต้องรู้เพื่อให้คุณสามารถอ่านและเขียน Helm Chart ได้เข้าใจมากขึ้น

### 📚 หัวข้อการเรียนรู้

1. [**พื้นฐาน Syntax และตัวแปร**](./01-syntax-basics.md): เข้าใจเครื่องหมาย `{{ }}`, การดึงค่าจาก `values.yaml` และการใช้ Pipeline
2. [**โครงสร้างควบคุม (Logic)**](./02-control-structures.md): การใช้ `if`, `with`, และการวนลูป `range`
3. [**Helpers และการ Reuse**](./03-helpers-and-includes.md): วิธีการใช้ `_helpers.tpl` และฟังก์ชั่น `include`

---

### 💡 คำแนะนำเพิ่มเติม

- ทุกครั้งที่เขียน Template แนะนำให้ลองรัน `helm template ./` เพื่อดูว่า YAML ที่ออกมาเป็นไปตามที่คิดไว้ไหม
- หาก Error ให้เช็คเรื่อง "Indent (การย่อหน้า)" เป็นอันดับแรก เพราะ YAML ซีเรียสเรื่องนี้มากครับ
